package application.TrackModel;

public interface TrackLineInterface {
	/*
	 * HOW TO USE: Write a function skeleton here that would make your life just a
	 * little bit easier.
	 * 
	 * I will ONLY write functions that will make it easier to use TrackModel
	 * objects. Anything complicated like calculating the distance from one block to
	 * another can be done using current methods.
	 * 
	 * Be sure to specify parameter and return type using the format below.
	 */

	/**
	 * [describe what this method needs to do and why]
	 * 
	 * @param [parameterName1] - [what the parameter is]
	 * @param [parameterName2] - [what the parameter is]
	 * @return [Type of object I am returning] - [what the return object is]
	 * @throws [exception type] - [when this exception would be thrown]
	 */
	// [ObjectTypeReturn] methodName([ObjectType1] [parameterName1], [ObjectType2] [parameterName2]);
}
